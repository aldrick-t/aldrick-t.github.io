{
  "name": "aldrick-t-portfolio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "content:check": "node ./scripts/validate-content.mjs",
    "new:item": "node ./scripts/new-item.mjs",
    "build:cv": "node ./scripts/build-cvs.mjs",
    "build": "npm run content:check && astro build",
    "build:all": "npm run content:check && npm run build:cv && astro build",
    "preview": "astro preview",
    "check": "npm run content:check && astro check"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.6",
    "astro": "^7.0.2"
  },
  "devDependencies": {
    "js-yaml": "^4.2.0",
    "typescript": "^5.9.3"
  }
}
